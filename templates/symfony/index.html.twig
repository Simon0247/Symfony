{% extends "base.html.twig"%}

{% block body %}
<html>
    <head>
        <title>Welcome to Symfony!</title>
    </head>
    <body>
        <h1>{{ page_title }}</h1>

        {% if app.user %}
            Hello {{ app.user.name }}!
        {% endif %}

        {# ... #}
    </body>
</html>
{% endblock %}