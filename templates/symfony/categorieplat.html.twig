{% extends "base.html.twig"%}

{% block body %}
	<main class="" container>
		<section class="" row>
			<div class="col-12 p-0">
				<img src="/media/bg2.jpeg" class="img-fluid w-100 h-auto"/>
				<h1>Liste des plats<h1>
						<div class="row">
							{% for plat in liste %}
								<div class="col-3 mb-3">
									<div class="card  mx-auto" style="width: 18rem; " src="..." alt="">
										<a href="{{ path('categorie', { 'id': plat.id }) }}" class="">
											<img src="/media/category/{{plat.image}}" class="btn btn card-img-top img-fluid p-10"/>
										</a>
									</div>
								</div>
							{% endfor %}
						</div>
					</section>
				</main>
			{% endblock %}